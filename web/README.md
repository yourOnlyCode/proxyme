## proxyme-web (legal + support site)

This is a small Next.js site to host required public URLs for app store submission:
- `/privacy`
- `/terms`
- `/support`

### Run locally

```bash
cd web
npm install
npm run dev
```

### Deploy
- Recommended: Vercel
- Add your domain and point `/privacy`, `/terms`, `/support` to this deployment.

